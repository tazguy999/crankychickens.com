[build]
  publish = "."
  functions = "netlify/functions"

# Let functions through before the catch-all redirect
[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
